<h2 class="page-header">Admin Dashboard Page</h2>
<br><br><br>
<h3 class="page-header">Recently Added</h3>

<%if (locals.pages.length > 0) {%>
    <div>
        <label>Pages</label>
        <br><br>
        <%pages.forEach(page => {%>
            <div><%=page.pageTitle%></div>
        <%})%>
    </div>
<%}else {%>
    <h3>Recently nothing change......</h3>
<%}%>
<br>
<%if (locals.categories.length > 0) {%>
    <div>
        <label>Categories</label>
        <br><br>
        <%categories.forEach(category => {%>
            <div><%=category.categoryName%></div>
        <%})%>
    </div>
<%}else {%>
    <h3>Recently nothing change about category ......</h3>
<%}%>
<br>
<%if (locals.products.length > 0) {%>
    <label>Products</label>
    <br><br>
    <%- include("../partials/productGrid", {products: products, large: true})%>
<%}else {%>
    <h3>Recently nothing change about products ......</h3>
<%}%>
